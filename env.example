# ============================================================================
# Embedding 配置
# ============================================================================

# Embedding 模型选择
# 推荐模型：
# - moka-ai/m3e-base (默认) - 中文优化，速度快，适合中文文本
# - moka-ai/m3e-large - 中文优化，精度更高但速度较慢
# - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 - 多语言，速度快
# - sentence-transformers/paraphrase-multilingual-mpnet-base-v2 - 多语言，精度高
# - BAAI/bge-large-zh-v1.5 - 中文优化，精度最高但速度最慢
# - BAAI/bge-base-zh-v1.5 - 中文优化，平衡性能和精度
EMBEDDING_MODEL=moka-ai/m3e-base

# Embedding 批处理大小（一次处理多少条文本）
# 建议值：100-500，根据可用内存调整
EMBEDDING_BATCH_SIZE=200

# Embedding 速率限制退避时间（秒）
# 当遇到速率限制时，等待多长时间后重试
EMBEDDING_RATE_LIMIT_BACKOFF=2.0

# Embedding 上下文长度限制（字符数）
# 超过此长度的文本会被截断
EMBEDDING_CONTEXT_LIMIT=6000

# Embedding 列名（数据库中的列名）
EMBEDDING_COLUMN=embedding_context

# Embedding 设备选择（可选）
# 可选值：cpu, cuda, mps
# 如果不设置，将自动检测可用设备
# EMBEDDING_DEVICE=cuda

# ============================================================================
# Supabase 配置
# ============================================================================

# Supabase 项目 URL
# 格式：https://your-project-id.supabase.co
SUPABASE_URL=

# Supabase 服务密钥（Service Role Key）
# 注意：这是敏感信息，不要提交到版本控制
# 可以在 Supabase 项目设置 -> API -> service_role key 中找到
SUPABASE_SERVICE_KEY=

# ============================================================================
# 数据库表配置
# ============================================================================

# 清理数据表名
CLEAN_DATA_TABLE_NAME=clean_data

